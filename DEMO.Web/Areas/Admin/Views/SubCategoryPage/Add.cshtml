@model DEMO.Dto.SubCategoryPageDto
@{
    Layout = "/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = Model.Id > 0 ? "Edit Category" : "Add Category";
}

<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-header bg-primary text-white">
            <h4 class="mb-0">@ViewData["Title"]</h4>
        </div>
        <div class="card-body">
            <form asp-action="Add" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="Id" />

                <div class="mb-3">
                    <label asp-for="SubName" class="form-label fw-bold"></label>
                    <input asp-for="SubName" class="form-control" placeholder="Enter sub-category name" />
                    <span asp-validation-for="SubName" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label for="CategoryId">Category</label>
                    @Html.DropDownListFor(model => model.CategoryId,
                    (SelectList)ViewBag.Categories,
                                        "Select Category",
                                        new { @class = "form-control" })
                </div>

                <div class="mb-3">
                    <label class="form-label fw-bold">Created Date</label>
                    <div>@Model.CreatedDate.ToString("yyyy-MM-dd")</div>  <!-- show date -->

                    <input type="hidden" name="CreatedDate" value="@Model.CreatedDate.ToString("yyyy-MM-dd")" />
                </div>

                <div class="mb-3">
                    <label class="form-label fw-bold">Status</label>
                    <div>@(Model.Status ? "Active" : "Inactive")</div>  <!-- show status -->

                    <input type="hidden" name="Status" value="@(Model.Status ? "true" : "false")" />
                </div>


                <div class="d-flex justify-content-between">
                    <a asp-area="Admin" asp-controller="DashBoard" asp-action="Home" class="btn btn-secondary">
                        <i class="bi bi-arrow-left"></i> Back to Home
                    </a>
                    <button type="submit" class="btn btn-success">
                        <i class="bi bi-save"></i> Save
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
